# FastSCP — 产品介绍(给设计师的 brief)

> 这是给设计 agent 的产品说明文档。请基于这份文档设计 App 图标、菜单图形、相关视觉资产。

## 一、产品是什么

**FastSCP** 是一个 macOS Finder 右键菜单扩展,核心功能:把选中的文件/文件夹**一键 SCP 传送到已配置的 SSH 服务器**。

它解决的具体问题:**开发者和运维每天要把本地的代码、日志、截图、配置传到服务器**。传统做法是开终端、敲 `scp` 命令(或用 FileZilla、Transmit 等 GUI 工具),每次都得手动输路径。FastSCP 把这个过程压到 **Finder 右键 → 一两下点击**。

## 二、目标用户

- **主要**:macOS 上的开发者、后端工程师、运维人员
- **次要**:经常需要在 Mac 和 Linux 服务器之间互传文件的科研/设计/产品人员
- **使用环境**:熟悉 `~/.ssh/config`、知道 SSH 别名是什么

## 三、核心卖点(设计图标时要把哪些视觉元素传达到位)

### 1. 速度感
- 一键发送到上次目标:**全程零弹窗**
- 自动记忆最近 5 个目标,直接列在右键菜单里
- 弹窗小(360×440)、快速定位、双击即进入

### 2. 集成感
- **读 `~/.ssh/config`** — 不维护第二份服务器列表
- 用系统 `scp` / `ssh` 进程,**完全继承**现有 SSH 认证、密钥、ProxyJump 等
- Finder 原生右键菜单,**不需要新开 app**

### 3. 简洁感
- 整个 app 没有任何"Dashboard"、"设置中心"、"账户" — 装上就用
- 单个小弹窗完成路径选择,不用学
- 没有花哨动画,macOS native 风格

## 四、当前形态

| 项 | 内容 |
|---|---|
| App 名称 | FastSCP |
| Bundle ID | com.zhuzhong.FastSCP |
| 最低系统 | macOS 14.0(Sonoma) |
| 技术栈 | Swift + SwiftUI + AppKit + Finder Sync |
| 开源协议 | MIT |
| 体积 | 框架 + 主 app + Finder 扩展 ≈ 小型(几 MB) |
| Dock 图标 | 无(LSUIElement agent app) |

## 五、右键菜单结构(请基于此考虑图形一致性)

```
右键任意文件/文件夹
└── FastSCP ▸
    ├── 发送到 server1:/var/www       ← 一键项(零弹窗)
    ├── 最近发送 ▸                    ← 子菜单(记忆的 5 个)
    │     ├── server2:/opt/app/logs
    │     └── server1:/tmp
    └── 传送到服务器…                  ← 弹小窗
```

弹窗长这样(供设计参考尺寸与布局):
```
┌─ 传送到服务器 ───────────┐
│ 服务器 [server1     ▾]   │  紧凑下拉
│ 路径   [/var/www/    ]   │  可键入,回车跳转
│ ┌─────────────────────┐ │
│ │ 📁 ..                  │ │  双击进入
│ │ 📁 html                │ │
│ │ 📁 logs                │ │
│ │ ...                    │ │
│ └─────────────────────┘ │
│ [发送 3 项 → server1:/... ] │
└─────────────────────────┘
```

## 六、隐喻与设计关键词

供图标设计参考的关键词和意象:

- **传输**(动词) — 箭头、流动、轨迹
- **服务器**(名词) — 服务器塔、机架、网络节点
- **Mac/Finder 风格**(形容词) — 简洁、原生、克制
- **开发者工具**(行业) — 终端、命令行、现代 SaaS(类似 Linear / Raycast / Warp / Tower)

候选隐喻方向(择一或组合):

1. **箭头 + 服务器塔**:稳重、企业感(类似 1Password / Tailscale 那种盾牌型)
2. **飞行轨迹 + 服务器**:速度感,最贴"fast"
3. **极简箭头 + 节点**:现代、克制,像终端命令符号

## 七、推荐调性

- **避免**:卡通、拟物化、游戏感、多彩渐变、emoji 风格
- **倾向**:几何化、扁平、单色主导、macOS Big Sur+ 风格、SF Symbols 的克制感
- **配色**:可以考虑 macOS 系统色(蓝/紫/橙),或开发工具经典的深色 + 亮色高亮;**避免**大红大绿

## 八、需交付的设计资产清单

1. **主 App icon**(最重要):
   - 1024×1024 源文件(PNG/SVG)
   - 自动生成 macOS 全部尺寸:16, 32, 64, 128, 256, 512, 1024
   - `.icns` 打包文件(可用 `iconutil` 从 iconset 生成)
   - 需考虑:圆角矩形 mask 由系统自动应用,源图通常提供方形带圆角或方形均可

2. **Finder 菜单图标**(可选,优先级低):
   - Finder Sync extension 显示在菜单旁边的图标
   - 尺寸:16pt 单色模板(类似 SF Symbols)

3. **README 顶图**(可选,用于开源宣传):
   - 横版,展示右键菜单效果
   - 1280×640 左右

## 九、品牌名与文案

- **名称**:FastSCP(全大写 SCP 是行业惯例 — Secure Copy Protocol,不要改成小写)
- **slogan 候选**:
  - "在 Finder 里,一键把文件送到服务器。"
  - "右键 → 传送 → 完成。"
  - "FastSCP. Right-click, sent."
- **README 现有标题**:FastSCP
- **菜单内文案**:已确定为"发送到"、"最近发送"、"传送到服务器…"(动词导向)

## 十、参考应用(设计对齐)

可以参考以下 macOS 开发工具的设计语言:

- **Transmit**(FTP 客户端)— 经典的双窗格图标
- **Tower**(Git 客户端)— 几何化、稳重
- **Warp**(终端)— 现代、极简、速度感
- **Linear / Raycast** — 简洁的 icon + 文字 logo
- **Tailscale** — 盾牌型几何图形

## 十一、设计交付方式

- 给我 PNG / SVG 源文件即可
- 我会用 `iconutil` 把 PNG iconset 打成 `.icns`,复制到 `FastSCP.app/Contents/Resources/AppIcon.icns`
- 如果设计 agent 直接给出 `.icns` 也行
- **不需要做 code 改动** — 我来处理集成

## 十二、其他需要关注的设计资产

- 项目 logo(纯文字版) — 可作为 README 头部装饰
- 截图占位符(README 里描述了 4-5 张图位)— 出最终成品时可拍真实截图替换

---

**联系方式**:`@zhuzhong`,项目地址 `/Users/zhuzhong/superconductor/projects/mac-fast-scp`,主分支 `main`。